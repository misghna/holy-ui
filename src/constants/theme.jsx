import { green, purple } from "@mui/material/colors";

export const theme = {
  palette: {
    mode: "light",
    primary: {
      main: purple[900]
    },
    secondary: {
      main: green[500]
    }
  }
};

export const DRAWER_WIDTH = 240;
